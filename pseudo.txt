do
	do
		start -> dot
		pointers from start
		if None: break
		select random
			push other on stack
	while true
	do
		check stack: if empty: return
		get branch pointer from stack
		(repeat if not safe)
	while pointer is not safe
while true

-----------------------------
safe pointer::

check dir for proximity
if ok cont
from x to y as i:
	if i === not zero:
		apply sequence 1,1,0,0 to source + 0 + dataSize offset
	else:
		apply sequence -1,1,-1,1
check all 4 candidates for proximity
